# to-do-list-api
A simple RESTful API for managing a to-do list. Include features like adding, updating, deleting, and retrieving tasks.

### Tech Stack
Gin (for routing), Gorm (for ORM), PostgreSQL (for database).


### Additional Features
JWT authentication, user registration and login, unit tests, and Docker for containerization.